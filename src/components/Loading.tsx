const Loading = () => {
  return (
    <div className="flex items-center justify-center h-screen">
      <div className="animate-spin rounded-full h-24 w-24 border-8 border-t-8 border-gray-200 border-t-slate-500"></div>
    </div>
  );
};

export default Loading;
